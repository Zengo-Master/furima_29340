<div class="whole">

  <header class="furima">
    <%= link_to image_tag("furima-logo-color.png", width: '200'), root_path %>
  </header>

  <div class="form-wrap-sell">

    <div class="form-header">
      <div class="form-header-sell">
        <p>商品情報入力</p>
      </div>
    </div>

    <div class="input-forms">
      <%= form_with(model: @item, local: true) do |f| %>

        <div class="input-field">
          <%= f.label :image, '出品画像', class: 'selling-image' %><span class="hissu">必須</span><br />
          <p class="selling-image-text">クリックしてファイルをアップロード</p>
          <%= f.file_field :image %>
        </div>

        <div class="input-field">
          <%= f.label :name, '商品名' %><span class="hissu">必須</span><br />
          <%= f.text_area :name, placeholder: '40字まで', size: "40x2" %>
        </div>

        <div class="input-field">
          <%= f.label :description, '商品の説明' %><span class="hissu">必須</span><br />
          <%= f.text_area :description, placeholder: '1000字まで', size: "40x5" %>
        </div>

        <div class="item-details">

          <div class="details-title">
            <p>商品の詳細</p>
          </div>

          <div class="details-types">

            <div class="category">
              <%= f.label :category, "カテゴリー" %><span class="hissu">必須</span><br />
              <%= f.collection_select :category, Category.all, :id, :name, {} %>
            </div><br>

            <div class="status">
              <%= f.label :status, "商品の状態" %><span class="hissu">必須</span><br />
              <%= f.collection_select :status, Status.all, :id, :name, {} %>
            </div>

          </div>

        </div>

        <div class="item-shipping">

          <div class="shipping-title">
            <p>配送について</p>
          </div>

          <div class="shipping-types">

            <div class="shipping_fee_burden">
              <%= f.label :shipping_fee_burden, "配送料の負担" %><span class="hissu">必須</span><br />
              <%= f.collection_select :shipping_fee_burden, Fee.all, :id, :name, {} %>
            </div><br>

            <div class="shipping_prefecture">
              <%= f.label :shipping_prefecture, "発送元の地域" %><span class="hissu">必須</span><br />
              <%= f.collection_select :shipping_prefecture, Prefecture.all, :id, :name, {} %>
            </div><br>

            <div class="shipping_days">
              <%= f.label :shipping_days, "発送までの日数" %><span class="hissu">必須</span><br />
              <%= f.collection_select :shipping_days, Days.all, :id, :name, {} %>
            </div>
 
          </div>

        </div>

        <div class="price">

          <div class="price-title">
            <p>販売価格</p>
            <p>(¥300〜9,999,999)</p>
          </div>

          <div class="price-calclation">
            <div class="price-items">

             <div class="price-row">
              <%= f.label :price, '価格' %><span class="hissu">必須</span>
              <span class="price-input">¥ <%= f.text_field :price, placeholder: '半角数字で入力' %></span>
             </div>

              <p class="price-row">販売手数料 (10%)<span id="yen"><span id="tax_price"%></span>円</p></span>

              <p class="price-row">販売利益<span id="yen"><span id="profit"></span>円</span></p>

            </div>

          </div>

        </div>

        <p><a href="#">禁止されている出品、行為</a>を必ずご確認ください。またブランド品でシリアルナンバー等がある場合はご記載ください。<a href="#">偽ブランドの販売</a>は犯罪であり処罰される可能性があります。</p>
        <p>また、出品をもちまして<a href="#">加盟店規約</a>に同意したことになります。</p>

        <div class="decide">
          <%= f.submit "変更する", class: 'decide-btn' %>
        </div>

        <div class="back">
          <%= link_to "戻る", item_path(@item.id), class: "back-btn" %>
        </div>

      <% end %>
    </div>

  </div>

  <footer class = "furima">
    <%= link_to image_tag("furima-logo-color.png", width: '200'), root_path %>
  </footer>

</div>